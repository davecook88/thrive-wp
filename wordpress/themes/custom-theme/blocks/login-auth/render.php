<?php
/**
 * Server-rendered Thrive Login/Auth block wrapper.
 * Reuses the existing PHP template logic for consistency front + editor.
 */
if (!defined('ABSPATH')) {
    exit;
}

require_once __DIR__ . '/login-auth.php';
