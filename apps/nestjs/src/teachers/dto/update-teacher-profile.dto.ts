/**
 * Teacher profile update DTO
 * Unified with shared package types to avoid duplication
 */
export {
  UpdateTeacherProfileSchema,
  LocationInputSchema,
} from "@thrive/shared";
export type { UpdateTeacherProfileDto, LocationInputDto } from "@thrive/shared";
